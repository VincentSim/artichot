// Here you will write *javascript* that would be executed by the browser
<% if @like.errors.any? %>
  // Render new review form with errors
  $('#like-div').html("<%= j render 'likes/form', art_piece: @art_piece, like: @like, user:current_user %>");
  console.log('erreur')
<% else %>
  console.log('pas derreur')
  $('#like-div').html("<%= j render 'likes/delete', art_piece: @art_piece, like: @like, user: current_user %>");

<% end %>