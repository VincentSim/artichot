<h1>Listing espaces</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @espaces.each do |espace| %>
      <tr>
        <td><%= espace.name %></td>
        <td><%= espace.description %></td>
        <td><%= espace.user %></td>
        <td><%= link_to 'Show', espace %></td>
        <td><%= link_to 'Edit', edit_espace_path(espace) %></td>
        <td><%= link_to 'Destroy', espace, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Espace', new_espace_path %>
